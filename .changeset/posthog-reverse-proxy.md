---
"@remoraflow/ui": patch
---

Proxy PostHog events through /ingest via Vercel rewrites to bypass ad blockers. Use named parameter rewrite syntax to route analytics requests through the app's own domain instead of external PostHog hosts.

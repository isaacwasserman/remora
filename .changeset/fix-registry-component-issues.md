---
"@isaacwasserman/remora": patch
---

Fix registry component display and styling issues: add missing StepParams cases for agent-loop, sleep, wait-for-condition, and end output; add distinct TypeBadge colors; fix scroll overflow with min-h-0; fix TypeScript strict mode errors; ensure panel is included in registry build.

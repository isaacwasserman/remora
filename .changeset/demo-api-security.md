---
"@remoraflow/ui": minor
---

Add CORS restriction and workflow validation to demo API. CORS now restricts to Vercel deployment URLs and localhost (dev). Workflows are validated server-side before execution using the core compiler.

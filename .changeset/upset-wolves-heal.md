---
---

Documentation-only change: add a dedicated "Streaming & Channels" guide covering `executeWorkflowStream`, the `channel` option, `WorkflowExecutionStateChannel`, `MemoryExecutionStateChannel`, debouncing, and custom channel implementations. No package changes.

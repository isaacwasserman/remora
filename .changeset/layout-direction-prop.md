---
"@remoraflow/ui": minor
---

Add `layout` prop to `WorkflowViewer` for controlling DAG direction (`"vertical"` or `"horizontal"`). Also export the `LayoutDirection` type and accept a `direction` parameter in `buildLayout`/`buildEditableLayout`.

---
demo: patch
---

Add shareable workflow URLs in demo app. Users can now click Share to compress their workflow and generate a copyable URL with ?workflow= query param. Shared links automatically preload the workflow when opened.

---
"@isaacwasserman/remora": minor
---

Add probe-based data extraction for large datasets and integrate with agent-loop steps. Extract-data steps now automatically use JMESPath-based probing when data exceeds size threshold, with built-in schema summarization for LLM guidance.

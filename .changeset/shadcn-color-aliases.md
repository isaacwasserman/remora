---
'@isaacwasserman/remora': minor
---

Update viewer colors to use shadcn design tokens. Replaces hardcoded gray colors with Tailwind aliases (bg-card, text-foreground, text-muted-foreground, border-border, etc.) for seamless downstream app design system compatibility. Resolves CSS variables for inline style colors with automatic fallbacks.

---
"@isaacwasserman/remora": patch
---

Group registry components into subdirectories. Components now install with directory structure preserved (nodes/, edges/, panels/) instead of flattening to a single directory. Import paths updated from @/registry/default/ to @/components/.

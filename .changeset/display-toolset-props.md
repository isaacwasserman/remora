---
"@remoraflow/core": minor
"@remoraflow/ui": minor
---

Add `DisplayToolSet` type with optional `displayName` and `displayDescription` per tool. The WorkflowViewer `tools` prop now accepts `DisplayToolSet`, rendering display overrides in place of real tool names and descriptions when provided.

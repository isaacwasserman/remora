---
"@remoraflow/ui": minor
---

Show a tool's output schema in the workflow editor. When editing a `tool-call` step, the params panel now displays the tool's declared `outputSchema` as JSON Schema below the inputs, so users can see what data will be available to downstream steps after the tool runs.

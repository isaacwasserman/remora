---
"@remoraflow/ui": minor
---

Add `@remoraflow/ui/theme-vars.css` export containing CSS custom properties (step tones, status colors, React Flow mappings, syntax highlighting) extracted from `theme.css`, so consumers can import variables independently of the Tailwind theme block.

export const NODE_WIDTH = 300;
export const NODE_HEIGHT = 180;
export const START_NODE_SIZE = 60;
export const GROUP_HEADER_WIDTH = 280;
export const GROUP_HEADER_HEIGHT = 80;
export const GROUP_PADDING = 30;
export const GROUP_HEADER = 40;
export const RANK_SEP = 60;
export const NODE_SEP = 40;
export const START_NODE_ID = "__start__";

export const EMPTY_GROUP_WIDTH = GROUP_HEADER_WIDTH + GROUP_PADDING * 2;
export const EMPTY_GROUP_HEIGHT =
    GROUP_HEADER_HEIGHT + GROUP_HEADER + GROUP_PADDING;

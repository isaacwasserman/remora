diff --git a/dist/index-CLddUTqr.js b/dist/index-CLddUTqr.js
index 88c56950697da05e8b8907269703ba29a47bc8be..28105429b86c1ad108e89f0d4e5ff784c02a543b 100644
--- a/dist/index-CLddUTqr.js
+++ b/dist/index-CLddUTqr.js
@@ -12,35 +12,25 @@ class MissingDependencyError extends Error {
   }
 }
 
+const vendorModules = {
+  arktype: './arktype-aI7TBD0R.js',
+  effect: './effect-QlVUlMFu.js',
+  sury: './sury-CWZTCd75.js',
+  typebox: './typebox-Dei93FPO.js',
+  valibot: './valibot--1zFm7rT.js',
+  zod: './zod-Bwrt9trS.js',
+};
+
 const getToJsonSchemaFn = async (vendor) => {
   const cached = validationMapper.get(vendor);
   if (cached) {
     return cached;
   }
-  let vendorFnPromise;
-  switch (vendor) {
-    case "arktype":
-      vendorFnPromise = (await import('./arktype-aI7TBD0R.js')).default();
-      break;
-    case "effect":
-      vendorFnPromise = (await import('./effect-QlVUlMFu.js')).default();
-      break;
-    case "sury":
-      vendorFnPromise = (await import('./sury-CWZTCd75.js')).default();
-      break;
-    case "typebox":
-      vendorFnPromise = (await import('./typebox-Dei93FPO.js')).default();
-      break;
-    case "valibot":
-      vendorFnPromise = (await import('./valibot--1zFm7rT.js')).default();
-      break;
-    case "zod":
-      vendorFnPromise = (await import('./zod-Bwrt9trS.js')).default();
-      break;
-    default:
-      throw new UnsupportedVendorError(vendor);
+  const modulePath = vendorModules[vendor];
+  if (!modulePath) {
+    throw new UnsupportedVendorError(vendor);
   }
-  const vendorFn = await vendorFnPromise;
+  const vendorFn = await (await import(modulePath)).default();
   validationMapper.set(vendor, vendorFn);
   return vendorFn;
 };
diff --git a/dist/index-aBd5wZAR.cjs b/dist/index-aBd5wZAR.cjs
index af6bfe83ef528734061c3bad362ba623d82f38c7..6c31548bf8bf0958db0933f015970c86392469c5 100644
--- a/dist/index-aBd5wZAR.cjs
+++ b/dist/index-aBd5wZAR.cjs
@@ -14,35 +14,25 @@ class MissingDependencyError extends Error {
   }
 }
 
+const vendorModules = {
+  arktype: './arktype-dE563TQ0.cjs',
+  effect: './effect-IriD2QG7.cjs',
+  sury: './sury-79aC8rnF.cjs',
+  typebox: './typebox-DYb9QD5O.cjs',
+  valibot: './valibot-DKumHSJb.cjs',
+  zod: './zod-BSYBZsr2.cjs',
+};
+
 const getToJsonSchemaFn = async (vendor) => {
   const cached = validationMapper.get(vendor);
   if (cached) {
     return cached;
   }
-  let vendorFnPromise;
-  switch (vendor) {
-    case "arktype":
-      vendorFnPromise = (await Promise.resolve().then(function () { return require('./arktype-dE563TQ0.cjs'); })).default();
-      break;
-    case "effect":
-      vendorFnPromise = (await Promise.resolve().then(function () { return require('./effect-IriD2QG7.cjs'); })).default();
-      break;
-    case "sury":
-      vendorFnPromise = (await Promise.resolve().then(function () { return require('./sury-79aC8rnF.cjs'); })).default();
-      break;
-    case "typebox":
-      vendorFnPromise = (await Promise.resolve().then(function () { return require('./typebox-DYb9QD5O.cjs'); })).default();
-      break;
-    case "valibot":
-      vendorFnPromise = (await Promise.resolve().then(function () { return require('./valibot-DKumHSJb.cjs'); })).default();
-      break;
-    case "zod":
-      vendorFnPromise = (await Promise.resolve().then(function () { return require('./zod-BSYBZsr2.cjs'); })).default();
-      break;
-    default:
-      throw new UnsupportedVendorError(vendor);
+  const modulePath = vendorModules[vendor];
+  if (!modulePath) {
+    throw new UnsupportedVendorError(vendor);
   }
-  const vendorFn = await vendorFnPromise;
+  const vendorFn = await (await Promise.resolve().then(function () { return require(modulePath); })).default();
   validationMapper.set(vendor, vendorFn);
   return vendorFn;
 };
